/// A `config` class containing the images' asset names used across several
/// widgets.
class ImageAssetNames {
  /// The `History of Me` key logo.
  static const keyLogo = "assets/images/History_Of_Me_Key_64px-01.png";
}
